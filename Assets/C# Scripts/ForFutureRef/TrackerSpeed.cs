using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TrackerSpeed : MonoBehaviour
{
    public float speed = 1.0f; //How fast will the enemy close the distance to the player?
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
